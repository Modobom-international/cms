{
  "name": "page-exporter",
  "version": "1.0.0",
  "description": "Page exporter for CMS",
  "main": "src/index.js",
  "scripts": {
    "build": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  }
}
